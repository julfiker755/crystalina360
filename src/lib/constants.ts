export const authKey = "auth_token";

export enum role {
  reviewer = "reviewer",
  performer = "performer",
  admin = "admin",
  user = "user",
}

// "signIn" | "forgot" | "verify" | "reset" | "success" | null;
